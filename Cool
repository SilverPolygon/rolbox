print("Remote script loaded from GitHub!")

local part = Instance.new("Part")
part.Anchored = true
part.Size = Vector3.new(10, 1, 10)
part.Position = Vector3.new(0, 5, 0)
part.BrickColor = BrickColor.new("Bright blue")
part.Parent = workspace
